apiVersion: v1
kind: Service
metadata:
  name: civic-os-kb
  labels:
    app: civic-os-kb
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: civic-os-kb
