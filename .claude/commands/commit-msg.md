Please look at the git staged files, write a one-line commit message, and put it on the clipboard. You can ignore Tests to make it more condensed. Don't just talk about what changed, but also _why_ it was changed.

Use `printf '%s' "message" | pbcopy` to avoid a trailing newline. 